public class Taxi extends VehicleType{

   public Taxi(){
      setWidth();
      setHeight();
      setAcceleration();
      setMinSpeed();
      setTopSpeed();
      setTopHandlingSpeed();
      setMinHandlingSpeed();
      setImage();
      setName();
   }
}